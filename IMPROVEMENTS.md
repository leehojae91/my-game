# 개선 진행 기록

싱글 홀덤(텍사스 홀덤 1인용 웹 게임)의 반복 개선 기록. 매 회차마다 디자인 · 로직 · 재미 관점에서 개선점을 찾아 반영하고 커밋한다.

## 검증 방법

브라우저 없이 엔진을 검증하는 하네스를 사용한다.

- 족보 판정 13종 (로열 스트레이트 플러시 ~ 하이카드, 휠 스트레이트 포함)
- 무작위 액션으로 여러 판을 돌려 칩 총합 보존 · 음수 칩 없음 · 사이드 팟 정상 분배 확인

---

## 0회차 — 기반 구현

| 항목 | 내용 |
| --- | --- |
| 구성 | `index.html` + `css/style.css` + `js/{cards,evaluator,ai,game,ui}.js`, 빌드 도구 없음 |
| 게임 | 텍사스 홀덤 노리밋, 나 + 컴퓨터 3명, 블라인드 500/1000, 시작 칩 100,000 |
| 엔진 | 7장 중 최고 5장 족보 판정, 4개 베팅 라운드, 최소 레이즈 규칙, 사이드 팟 분배 |
| 상대 | 몬테카를로 승률 계산 + 성향 4종(신중 · 느슨 · 공격 · 침착) |
| 화면 | 펠트 테이블, 좌석 4개, 딜러 버튼, 베팅 칩 표시, 진행 기록, 규칙 창 |
| 편의 | 20초 턴 타이머, 베팅 프리셋(최소/하프/팟/올인), 단축키 F·C·R, 칩 저장 |

검증 결과: 족보 판정 통과, 40판 진행 통과 (21판째 참가자 부족으로 조기 종료).

---

## 1회차 — 노리밋 규칙 정확도

찾은 문제

1. **언더 올인이 액션을 재개시켰다.** 최소 레이즈에 못 미치는 올인(예: 최소 레이즈 2,000인데 남은 칩이 700이라 올인)이 들어와도 이미 액션을 마친 사람 전원의 액션이 되살아났다. 노리밋 규칙상 이 경우 베팅은 재개되지 않고, 앞서 액션한 사람은 콜 또는 다이만 할 수 있다.
2. **아무도 받지 않은 초과 베팅이 팟에 남았다.** 10,000을 걸었는데 상대가 3,000 올인만 받고 나머지가 전원 다이하면 차액 7,000은 즉시 돌려받아야 한다. 기존에는 팟에 들어간 채 쇼다운까지 끌고 갔다.

고친 내용

| 파일 | 변경 |
| --- | --- |
| `js/game.js` | 참가자별 `raiseLocked` 상태 추가. 언더 올인이면 이미 액션한 사람을 잠그고, 정상 레이즈면 잠금을 푼다 |
| `js/game.js` | `legalFor`가 잠긴 사람에게 레이즈를 막고, 잠긴 상태에서 들어온 레이즈 요청은 콜로 안전하게 처리 |
| `js/game.js` | `returnUncalled()` 추가 — 베팅 라운드가 끝날 때마다 최고 베팅과 2위 베팅의 차액을 되돌려 준다 |
| `js/ai.js` | 엔진이 계산한 `canRaise`를 그대로 사용하도록 변경 (기존에는 자체 추정이라 잠금 상태를 몰랐다) |

검증 결과: 족보 판정 통과, 40판 진행 통과. 실제로 초과 베팅 반환 17회 · 언더 올인 레이즈 잠금 1회가 발동했고 칩 총합은 계속 보존됐다.

---

## 2회차 — 판단 근거를 보여 주는 정보 표시

찾은 문제

1. **승률을 내 차례에만, 그것도 글자로만 보여 줬다.** 카드가 깔릴 때마다 상황이 어떻게 바뀌는지 따라가기 어려웠다.
2. **드로 정보가 전혀 없었다.** 플러시 한 장 남은 상황인지, 완전히 죽은 패인지 초보자가 알 수 없었다.
3. **프리플랍에 정보가 비어 있었다.** 카드 두 장만 덩그러니 있고 그게 좋은 시작 패인지 알 방법이 없었다.

고친 내용

| 파일 | 변경 |
| --- | --- |
| `js/evaluator.js` | `holeNotation()` 추가 — 시작 패를 `AKs` · `77` · `T2o` 형태로 표기 (10은 T로 표기) |
| `js/evaluator.js` | `analyzeDraws()` 추가 — 남은 카드를 한 장씩 대입해 족보가 오르는 카드 수를 세고 플러시 드로 · 양방향 스트레이트 드로를 구분 |
| `js/ui.js` | 승률을 막대그래프로 상시 표시, 60% 이상 초록 · 35% 이상 노랑 · 그 아래 빨강 |
| `js/ui.js` | 승률 계산 결과를 패·보드·인원 조합으로 캐시해 매 렌더마다 다시 계산하지 않도록 함 |
| `css/style.css` | 승률 막대 · 시작 패 표기 · 드로 안내 스타일 추가 |

검증 결과: 표기 4종, 드로 판정 4종(플러시 드로 · 양방향 스트레이트 드로 · 아웃츠 개수 · 리버에서 미표시) 통과.

---

## 3회차 — 상대가 사람처럼 두게 만들기

찾은 문제

1. **자리를 몰랐다.** 가장 먼저 말해야 하는 자리든 마지막에 말하는 버튼이든 똑같이 판단했다. 포커에서 자리는 패만큼 중요하다.
2. **상대의 공격을 무시했다.** 앞에서 누가 크게 올렸어도 무작위 상대를 가정한 승률만 보고 들어갔다.
3. **프리플랍이 뭉개졌다.** 카드 두 장만으로 돌린 승률은 서로 비슷하게 나와서, 좋은 시작 패와 나쁜 시작 패를 거의 구분하지 못했다.
4. **주도권 개념이 없었다.** 프리플랍에서 자기가 올려 놓고 플랍에서 그냥 체크하는, 사람이라면 안 할 행동을 했다.

고친 내용

| 파일 | 변경 |
| --- | --- |
| `js/ai.js` | `startingScore()` 추가 — Chen 방식 시작 패 점수로 프리플랍 참여선을 정한다. 자리가 늦을수록 참여선이 낮아진다 |
| `js/ai.js` | 이번 라운드 레이즈 횟수만큼 자기 승률을 깎아 본다 (`eq × 0.93^레이즈수`) |
| `js/ai.js` | 자리 점수로 공격성 · 요구 승률 · 허풍 빈도를 조정 |
| `js/ai.js` | 프리플랍 주도권을 잡았으면 플랍에서 이어 치는 행동 추가, 아주 강한 패는 가끔 체크로 유인 |
| `js/game.js` | `positionScore()` · `raisesThisStreet` · `preflopAggressor` 추적을 추가해 판단 재료를 넘긴다 |

검증 결과: 40판 액션 분포가 다이 29% · 체크 25% · 콜 26% · 레이즈 20%로, 한쪽에 쏠리지 않고 균형이 잡혔다.

---

## 4회차 — 실제 브라우저에서 확인하고 고치기

브라우저로 직접 띄워 한 판을 끝까지 돌려 보고 눈에 보이는 문제를 잡았다.

찾은 문제

1. **체크와 콜 버튼이 동시에 떴다.** `showActionButtons(true)`가 개별 표시 설정을 덮어써서, 콜해야 하는 상황에 체크 버튼도 같이 보였다.
2. **좌석 이름이 세로로 깨졌다.** 좌석 폭이 모자라 "마돌이"가 한 글자씩 세로로 쌓이고 칩 숫자가 카드에 잘렸다.
3. **결과 배너가 보드 카드를 가렸다.** 정작 승부가 갈린 카드 5장을 볼 수 없었다.
4. **사이드 팟이 쓸데없이 쪼개졌다.** 다이한 사람의 기여액이 팟 경계를 만들어, 올인이 하나도 없는데도 팟이 3개로 나뉘고 결과창에 같은 줄이 세 번 반복됐다.
5. **매 렌더마다 카드 DOM을 새로 만들었다.** 타이머가 0.2초마다 좌석 전체를 다시 그려 보드 카드 등장 애니메이션이 반복 재생됐다.

고친 내용

| 파일 | 변경 |
| --- | --- |
| `js/ui.js` | 버튼 표시 순서 교정, 레이즈 불가 상황에서 레이즈 버튼 숨김 |
| `js/ui.js` | 보드를 증분 렌더로 바꿔 새로 깔린 카드만 애니메이션. 타이머는 막대 폭만 직접 갱신 |
| `js/ui.js` | 승리에 쓰인 5장을 금색으로 강조, 획득 금액을 좌석 위로 띄우는 연출, 팟 금액 카운트업 |
| `js/ui.js` | 승자가 같은 팟은 한 줄로 합산 표시 |
| `js/game.js` | `buildPots()`에서 받을 사람이 같은 구간을 병합 — 올인이 있을 때만 팟이 갈린다 |
| `css/style.css` | 좌석 폭 확대, 이름 줄바꿈 방지, 성향 배지를 좌석 위로 분리, 배너를 보드 위로 이동 |

검증 결과: 브라우저에서 한 판 자동 진행 후 보드 5장 · 강조 5장 · 결과 1줄로 정상 표시. 엔진 테스트도 그대로 통과.

---

## 5회차 — 계속하게 만드는 요소

찾은 문제

1. **한 판이 끝날 때마다 버튼을 눌러야 했다.** 흐름이 계속 끊긴다.
2. **상대가 생각하는 시간이 고정이었다.** 익숙해진 뒤에는 답답하다.
3. **쌓이는 게 아무것도 없었다.** 몇 판을 했는지, 얼마를 벌었는지, 어떤 족보까지 만들어 봤는지 남지 않아 계속할 이유가 약했다.

고친 내용

| 파일 | 변경 |
| --- | --- |
| `js/game.js` | 진행 속도 배율 `speed`와 `pace()` 도입 — 상대 생각 시간, 카드 공개 간격에 일괄 적용 |
| `js/ui.js` | 자동 진행 토글 — 판이 끝나면 잠시 뒤 다음 판이 알아서 시작 |
| `js/ui.js` | 속도 토글 (보통 · 빠름), 두 설정 모두 저장 |
| `js/ui.js` | 누적 전적 — 참가한 판 · 이긴 판과 승률 · 쇼다운 비율 · 한 판 최대 획득 · 최고 족보 · 누적 손익 |
| `index.html`, `css/style.css` | 전적 창과 상단 토글 버튼 추가 |

검증 중 헷갈린 점: 판 번호가 저장돼 이어지는 탓에 통계가 어긋나 보였다. 저장소를 비우고 다시 측정해 실행 4회 · 종료 4회 · 중복 실행 0회 · 집계 4판으로 일치하는 것을 확인했다.

---

## 6회차 — 좁은 화면 대응과 계산 정확도

먼저 성능을 재 봤다. 족보 판정은 초당 28만 회, 승률 계산 한 번은 4밀리초. 아낄 이유가 없어서 **반대로 시뮬 횟수를 늘려 정확도를 올렸다**.

찾은 문제

1. **휴대폰 폭에서 좌우 좌석이 커뮤니티 카드 위로 올라탔다.** 좌석 배치가 넓은 화면 기준 고정이었다.
2. **화면 오른쪽이 잘렸다.** grid 항목은 기본적으로 내용보다 작아지지 않아서(`min-width: auto`) 상단 바가 화면을 밀어냈다.
3. **버튼 글자가 세로로 깨졌다.** "자동 진행 켬"이 세 줄로 쪼개졌다.
4. **전적 창을 열어도 뒤에서 게임이 계속 돌았다.** 자동 진행 중에는 창을 보는 사이에 판이 넘어갔다.

고친 내용

| 파일 | 변경 |
| --- | --- |
| `js/ai.js` | 시뮬 횟수를 240~420회에서 400~800회로 상향 (판단 흔들림 감소) |
| `js/ui.js` | 표시용 승률도 800~1,200회로 상향 |
| `js/ui.js` | 창을 열면 자동 진행을 멈추고 닫으면 이어서 진행, ESC로 닫기 |
| `js/ui.js` | 좁은 화면에서 토글 버튼 라벨을 짧게 바꾸고 창 크기 변화에 반응 |
| `css/style.css` | `#app > * { min-width: 0 }`으로 넘침 차단, 상단 바 축소 규칙 |
| `css/style.css` | 780px 이하에서 상대 셋을 위쪽 반원에 배치하고 베팅 칩 · 딜러 버튼 · 결과 배너 위치를 함께 조정 |

검증 결과: 414 × 850 화면에서 가로 넘침 0, 좌석과 보드 겹침 없음. 1440 × 900에서도 기존 배치 유지.

---

## 7회차 — 긴장감과 캐릭터

찾은 문제

1. **올인이 밋밋했다.** 전 재산을 걸어도 카드가 조용히 깔리고 결과만 나왔다. 포커에서 가장 뜨거운 순간인데 아무 일도 일어나지 않았다.
2. **상대가 숫자 덩어리였다.** 이름과 칩만 있고 성격이 드러나지 않아 누구와 싸우는지 느껴지지 않았다.

고친 내용

| 파일 | 변경 |
| --- | --- |
| `js/ai.js` | `knownEquity()` 추가 — 서로의 패가 공개된 상태의 진짜 승률. 남은 카드가 1~2장이면 모든 경우를 세고, 그보다 많으면 1,500회 표본을 뽑는다 |
| `js/game.js` | 액션할 사람이 없어지면 카드를 공개하고 "올인 대결" 신호를 보낸 뒤 남은 카드를 천천히 깐다 |
| `js/game.js` | 올인 · 큰 레이즈 · 다이 · 승리 시 상대가 한마디 하도록 `onTalk` 신호 추가 |
| `js/ui.js` | 올인 대결 중 각 좌석에 실시간 승률 표시, 보드가 바뀔 때만 다시 계산 |
| `js/ui.js` | 성향별로 다른 대사가 섞여 나오는 말풍선 |
| `js/ui.js` | 연출용 요소를 좌석이 아니라 테이블 위 레이어에 띄우도록 변경 — 좌석 재렌더에 지워지던 문제 해결 |

검증 결과

- 승률 계산 정확도: AA 대 KK 프리플랍 83% 대 17% (알려진 값 81.9% 대 18.1%), 계산 시간 18밀리초
- 60판 자동 진행에서 올인 대결 14회 · 상대 대사 67회 발동
- 브라우저에서 올인 상황을 유도해 카드 공개 → 좌석별 승률 5% 대 95% 표시 → 리버 역전까지 정상 동작 확인

---

## 8회차 — 상대를 읽는 재미, 그리고 그 과정에서 드러난 결함

상대별 성향 수치를 붙였더니 **성향 설정이 실제 판단에 거의 반영되지 않고 있다는 사실이 드러났다.**

측정값 (80판)

| 상대 | 참여율 | 프리플랍 레이즈율 | 공격성 |
| --- | --- | --- | --- |
| 신중형 | 23% | 4% | 20% |
| 느슨형 | 15% | 3% | 48% |
| 공격형 | 15% | 1% | 58% |

신중형이 느슨형보다 더 많이 참여하고, 프리플랍 레이즈는 사실상 없었다. 원인은 두 가지였다.

1. **프리플랍 참여선이 상수였다.** `openLine = 9 - 자리 × 3`으로, 성향 값을 아예 쓰지 않았다.
2. **오픈 레이즈 경로가 없었다.** 빅블라인드가 놓인 상태는 `toCall > 0`이라 "아무도 안 걸었을 때" 분기로 가지 않고, 남은 경로는 재레이즈 조건(`점수 ≥ 참여선 + 5`)뿐이라 거의 항상 림프나 다이로 끝났다.

고친 내용

| 파일 | 변경 |
| --- | --- |
| `js/ai.js` | 참여선을 성향에서 계산 (`tight × 14 − 자리 × 2.5 + 레이즈수 × 2.5`) |
| `js/ai.js` | 아직 아무도 올리지 않은 상황을 따로 구분해 오픈 레이즈 경로를 추가 |
| `js/ai.js` | 성향별 `tight` 값을 더 벌려 실제 차이가 드러나게 조정 |
| `js/game.js` | 참여율 · 레이즈율 · 공격성 누적 집계. 규칙상 강등된 액션은 실제 결과 기준으로 기록 |
| `js/ui.js`, `index.html` | 전적 창에 상대 분석 표 추가, 승률·드로 안내를 끄는 힌트 토글 추가 |

수정 후 (150판)

| 상대 | 참여율 | 프리플랍 레이즈율 | 공격성 |
| --- | --- | --- | --- |
| 신중형 | 19% | 7% | 35% |
| 느슨형 | 27% | 19% | 48% |
| 공격형 | 20% | 15% | 51% |

성향별로 수치가 갈리고, 프리플랍 레이즈율도 실제 포커의 통상 범위에 들어왔다.

---

## 9회차 — 처음 열었을 때와 손에 익을 때

찾은 문제

1. **처음 열면 뭘 해야 할지 알 수 없었다.** 규칙 창은 있지만 스스로 찾아 눌러야 했다.
2. **판 시작이 밋밋했다.** 카드가 아무 동작 없이 그냥 나타났다.
3. **단축키가 있는데 아무도 모른다.** 코드에만 있고 화면에는 안내가 없었다.
4. **다이한 뒤가 지루했다.** 이미 빠진 판인데 남은 진행을 평소 속도로 끝까지 봐야 했다.

고친 내용

| 파일 | 변경 |
| --- | --- |
| `js/ui.js` | 저장된 기록이 없으면(첫 방문) 규칙 창을 먼저 띄운다 |
| `js/ui.js` | 판 시작 직후 한 번만 카드가 날아와 놓이는 애니메이션. 좌석·장수별로 시차를 준다 |
| `js/ui.js` | 다이하면 남은 진행을 0.28배 속도로 넘기고, 판이 끝나면 원래 속도로 복구 |
| `index.html`, `css/style.css` | 조작부에 단축키 안내 줄, 규칙 창에 단축키 항목, 키보드 이동 시 초점 표시 |

검증 결과: 저장소를 비우고 열었을 때 규칙 창 자동 표시, 판 시작 시 딜링 애니메이션 8장(4명 × 2장)이 첫 렌더에만 붙고 이후 렌더에서는 사라지는 것을 확인.

---

## 10회차 — 난이도, 그리고 "실력"을 어떻게 만들 것인가

초급 · 중급 · 고급을 넣었다. 판돈 크기와 상대 구성, 상대 실력이 함께 바뀐다.

| 난이도 | 블라인드 | 상대 실력 | 상대 성향 |
| --- | --- | --- | --- |
| 초급 | 250 / 500 | 0.5 | 신중 · 느슨 · 침착 |
| 중급 | 500 / 1,000 | 0.8 | 넷 모두 |
| 고급 | 1,000 / 2,000 | 1.0 | 공격 · 침착 · 신중 |

### 실패한 첫 시도

처음에는 실력이 낮으면 **판단이 흔들리게** 만들었다. 시뮬 표본을 줄이고 무작위 흔들림을 키우는 방식이다. 그런데 좌석까지 바꿔 가며 800판을 돌려 보니 결과가 거꾸로 나왔다.

```
800판 손익 — 실력 1.0: -126BB / 실력 0.5: +126BB
```

흔들림이 크다는 것은 가끔 크게 베팅한다는 뜻이고, 상대들이 자주 접는 환경에서는 그게 오히려 이득이었다. "부정확함"은 "약함"이 아니었다.

### 고친 방법

부정확함 대신 **손해가 확정된 실수**를 확률적으로 주입했다.

- 접어야 할 자리에서 따라간다 (다이 판단을 콜로 뒤집음)
- 충분히 좋은 패(승률 60% 초과)를 접는다

실수 확률은 `(1 − 실력) × 0.35`다. 실력 1.0은 실수하지 않는다.

```
800판 손익 — 실력 1.0: +188BB / 실력 0.5: -188BB
```

방향이 바로잡혔다.

기타 수정: 난이도를 바꿔도 상단 버튼 라벨이 그대로였다. 라벨 갱신 함수가 초기화 함수 안에 갇혀 있어서 밖에서 부를 수 없었던 것이라, 모듈 범위로 옮겼다.

---

## 마무리

10회차까지의 결과를 한 줄로 요약하면 이렇다.

| 회차 | 한 일 |
| --- | --- |
| 0 | 홀덤 엔진과 화면 기반 구현 |
| 1 | 노리밋 규칙 정확도 — 언더 올인 재오픈 금지, 초과 베팅 반환 |
| 2 | 승률 막대 · 드로 안내 · 시작 패 표기 |
| 3 | 상대 판단에 자리 · 주도권 · 상대 공격성 반영 |
| 4 | 브라우저 실측으로 UI · 사이드 팟 정산 문제 수정 |
| 5 | 자동 진행 · 속도 조절 · 누적 전적 |
| 6 | 모바일 레이아웃, 계산 정확도 상향 |
| 7 | 올인 대결 연출과 상대 캐릭터 대사 |
| 8 | 상대 분석 통계, 그 과정에서 드러난 프리플랍 판단 결함 수정 |
| 9 | 첫 방문 안내 · 딜링 연출 · 단축키 · 다이 후 가속 |
| 10 | 난이도 도입과 "실력" 구현 방식 교정 |

반복하면서 확인한 것: **눈에 보이게 만들면 숨어 있던 결함이 드러난다.** 상대 통계를 화면에 띄우자 성향이 판단에 반영되지 않고 있다는 사실이 드러났고(8회차), 난이도를 수치로 검증하자 "부정확함"과 "약함"이 다르다는 것이 드러났다(10회차). 두 경우 모두 표시 기능을 만들지 않았다면 계속 몰랐을 문제였다.

---

# 베팅 조작 개선

실제 포커 게임들과 비교해 베팅 조작을 다듬은 기록.

## 1회차 — 세 번 눌러야 베팅되던 것을 한 번으로

### 레퍼런스와 비교

| | 다른 포커 게임 | 개선 전 이 게임 |
| --- | --- | --- |
| 프리셋 버튼 | 누르면 그 금액으로 **바로 베팅** | 슬라이더 값만 바뀜 |
| 금액 표시 | 버튼에 **실제 금액이 찍혀 있음** ("하프 12,000") | "하프"라고만 적혀 있음 |
| 베팅까지 필요한 조작 | 1번 | 3번 (프리셋 → 슬라이더 확인 → 레이즈) |
| 팟 대비 비율 | 1/2 · 3/4 · 팟 등 여러 단계 | 하프 · 팟 두 단계 |

베팅액을 알려면 슬라이더 아래 작은 글씨를 봐야 했고, 확정하려면 오른쪽 끝 레이즈 버튼까지 눈과 손을 옮겨야 했다.

### 고친 내용

| 파일 | 변경 |
| --- | --- |
| `index.html` | 프리셋을 최소 · 1/2 팟 · 3/4 팟 · 팟 · 올인 다섯 단계로 늘리고, 각 버튼을 이름 + 금액 두 줄 구조로 변경 |
| `js/ui.js` | `presetTarget()` 분리 — 프리셋별 목표 금액 계산을 한 곳에서 처리 |
| `js/ui.js` | 프리셋 클릭이 곧 베팅 실행. 확인 단계 없음 |
| `js/ui.js` | `updateBetPanel()` — 내 차례가 되면 각 버튼에 실제 금액을 찍고, 남은 칩 전부에 해당하는 프리셋은 올인 색으로 표시. 레이즈 불가면 버튼 비활성 |
| `js/ui.js` | 레이즈 버튼에도 금액 표시 (`레이즈 14,498`), 슬라이더를 움직이면 함께 갱신 |
| `css/style.css` | 프리셋 버튼을 두 줄 구조로 키우고 올인 강조색 추가 |

슬라이더는 세밀 조정용으로 남겼다.

### 검증

프리플랍 블라인드 1,000/2,000, 팟 3,000, 콜 2,000 상황에서 계산 결과

| 프리셋 | 표시 금액 | 계산 |
| --- | --- | --- |
| 최소 | 4,000 | 현재 베팅 2,000 + 최소 인상폭 2,000 |
| 1/2 팟 | 4,500 | 콜 지점 2,000 + (팟 3,000 + 콜 2,000) × 0.5 |
| 3/4 팟 | 5,750 | 콜 지점 2,000 + 5,000 × 0.75 |
| 팟 | 7,000 | 콜 지점 2,000 + 5,000 |
| 올인 | 94,072 | 남은 칩 전부 |

실제로 눌렀을 때 기록에 `나 레이즈 4,000` · `나 벳 4,000`이 남아 버튼 표시 금액과 실제 베팅액이 일치하는 것을 확인했다.

---

## 2회차 — 기다리지 않게, 손이 덜 움직이게

### 레퍼런스와 비교

| | 다른 포커 게임 | 개선 전 이 게임 |
| --- | --- | --- |
| 내 차례 전 | 미리 액션을 걸어 두면 차례가 오는 즉시 처리 | 차례가 올 때까지 기다렸다가 눌러야 함 |
| 버튼 배치 | 베팅 프리셋이 액션 버튼 **바로 위**에 정렬 | 프리셋은 가운데, 액션은 오른쪽 끝 — 시선과 손이 좌우로 이동 |
| 슬라이더 | 필요할 때만 펼침 | 항상 자리를 차지 |

### 고친 내용

| 파일 | 변경 |
| --- | --- |
| `index.html` | 프리셋 · 미리 액션 · 액션 버튼을 `.bet-area` 한 덩어리로 묶어 세로로 쌓음 |
| `index.html` | 프리셋 행 끝에 "직접 조절" 버튼 추가 — 누를 때만 슬라이더가 펼쳐진다 (설정 저장) |
| `js/ui.js` | 미리 액션 추가 — `체크 · 다이`, `콜`. 내 차례가 아니고 아직 판에 살아 있을 때만 나타난다 |
| `js/ui.js` | 예약된 액션은 차례가 오면 0.34초 뒤 자동 실행되고 예약은 해제된다. 직접 액션하거나 새 판이 시작돼도 해제 |
| `css/style.css` | 프리셋 행과 액션 행의 폭을 맞춰 세로로 정렬 |

### 모바일에서 추가로 잡은 것

414 폭에서 확인하다 두 가지가 더 나왔다.

1. **보유 칩이 `84.0`으로 잘렸다.** 상단 바 항목이 많아 칩 표시가 밀려났다. 판수 배지와 자주 쓰지 않는 버튼(소리 · 초기화)을 좁은 화면에서 접고, 칩 배지는 줄어들지 않도록 고정했다.
2. **말풍선이 테이블 밖으로 나갔다.** 좌석 중심을 그대로 쓰다 보니 가장자리 좌석의 말풍선이 화면 밖으로 밀렸다. 테이블 안쪽으로 가두도록 위치를 제한했다.

### 검증

- 미리 액션 `콜` 예약 → 차례가 오자 기록에 `나 콜 2,000`이 남고 예약 표시가 해제되는 것 확인
- 프리셋 행 오른쪽 끝과 액션 행 오른쪽 끝 좌표가 1147px로 일치
- 414 × 850에서 가로 넘침 0, 보유 칩 `84,072` 온전히 표시
- 엔진 40판 회귀 통과

---

## 3회차 — 빨라진 만큼 생긴 위험을 막기

원터치로 바꾸고 나니 **누르면 곧바로 칩이 나간다**는 새 위험이 생겼다. 다른 게임들이 어떻게 막는지 보고 그 장치들을 가져왔다.

### 레퍼런스와 비교

| | 다른 포커 게임 | 개선 전 이 게임 |
| --- | --- | --- |
| 연타 보호 | 액션 직후 잠깐 입력을 받지 않음 | 없음 — 연타가 다음 차례로 흘러갈 수 있음 |
| 시간 임박 | 막대 색이 바뀌고 소리로 알림 | 막대만 조용히 줄어듦 |
| 콜 판단 근거 | 팟 오즈를 함께 표시 | 필요 금액만 표시 |
| 정밀 조작 | 휠 · 키보드로 금액 조절 | 슬라이더 드래그만 |

### 고친 내용

| 파일 | 변경 |
| --- | --- |
| `js/ui.js` | 액션 직후 350밀리초, 버튼이 나타난 직후 260밀리초 입력 잠금. 예약 액션 실행은 잠금과 무관하게 처리 |
| `js/ui.js` | 남은 시간 5초부터 막대가 빨갛게 변하고 1초마다 소리로 알림 |
| `js/ui.js` | 콜해야 할 때 팟 오즈를 함께 표시 (힌트 켬일 때만) |
| `js/ui.js` | 슬라이더 휠 조작, 숫자키 1~5로 프리셋 즉시 베팅 |
| `index.html` | 단축키 안내와 규칙 창에 숫자키 항목 추가 |

### 검증

| 확인 항목 | 결과 |
| --- | --- |
| 프리셋 3연타 | 기록에 `나 레이즈 4,000` 한 줄만 남음 |
| 휠 조작 | 4,000 → 5,000, 레이즈 버튼 라벨도 함께 갱신 |
| 숫자키 3 (3/4 팟) | 표시 금액 5,000 → `나 레이즈 5,000` |
| 팟 오즈 | 콜 2,000 · 팟 3,000 상황에서 40% 표시 |
| 엔진 회귀 | 40판 통과 |

---

## 베팅 개선 요약

| | 개선 전 | 개선 후 |
| --- | --- | --- |
| 베팅까지 조작 횟수 | 3번 | 1번 |
| 금액 확인 | 슬라이더 아래 작은 글씨 | 버튼마다 금액 표시 |
| 프리셋 단계 | 2단계 | 5단계 |
| 내 차례 대기 | 기다렸다가 눌러야 함 | 미리 정해 두면 자동 처리 |
| 버튼 배치 | 좌우로 흩어짐 | 세로로 정렬 |
| 오조작 | 막는 장치 없음 | 입력 잠금 · 시간 임박 경고 |
| 조작 수단 | 마우스 드래그 | 클릭 · 숫자키 · 휠 · 단축키 |
