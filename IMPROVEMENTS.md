# 개선 진행 기록

싱글 홀덤(텍사스 홀덤 1인용 웹 게임)의 반복 개선 기록. 매 회차마다 디자인 · 로직 · 재미 관점에서 개선점을 찾아 반영하고 커밋한다.

## 검증 방법

브라우저 없이 엔진을 검증하는 하네스를 사용한다.

- 족보 판정 13종 (로열 스트레이트 플러시 ~ 하이카드, 휠 스트레이트 포함)
- 무작위 액션으로 여러 판을 돌려 칩 총합 보존 · 음수 칩 없음 · 사이드 팟 정상 분배 확인

---

## 0회차 — 기반 구현

| 항목 | 내용 |
| --- | --- |
| 구성 | `index.html` + `css/style.css` + `js/{cards,evaluator,ai,game,ui}.js`, 빌드 도구 없음 |
| 게임 | 텍사스 홀덤 노리밋, 나 + 컴퓨터 3명, 블라인드 500/1000, 시작 칩 100,000 |
| 엔진 | 7장 중 최고 5장 족보 판정, 4개 베팅 라운드, 최소 레이즈 규칙, 사이드 팟 분배 |
| 상대 | 몬테카를로 승률 계산 + 성향 4종(신중 · 느슨 · 공격 · 침착) |
| 화면 | 펠트 테이블, 좌석 4개, 딜러 버튼, 베팅 칩 표시, 진행 기록, 규칙 창 |
| 편의 | 20초 턴 타이머, 베팅 프리셋(최소/하프/팟/올인), 단축키 F·C·R, 칩 저장 |

검증 결과: 족보 판정 통과, 40판 진행 통과 (21판째 참가자 부족으로 조기 종료).

---

## 1회차 — 노리밋 규칙 정확도

찾은 문제

1. **언더 올인이 액션을 재개시켰다.** 최소 레이즈에 못 미치는 올인(예: 최소 레이즈 2,000인데 남은 칩이 700이라 올인)이 들어와도 이미 액션을 마친 사람 전원의 액션이 되살아났다. 노리밋 규칙상 이 경우 베팅은 재개되지 않고, 앞서 액션한 사람은 콜 또는 다이만 할 수 있다.
2. **아무도 받지 않은 초과 베팅이 팟에 남았다.** 10,000을 걸었는데 상대가 3,000 올인만 받고 나머지가 전원 다이하면 차액 7,000은 즉시 돌려받아야 한다. 기존에는 팟에 들어간 채 쇼다운까지 끌고 갔다.

고친 내용

| 파일 | 변경 |
| --- | --- |
| `js/game.js` | 참가자별 `raiseLocked` 상태 추가. 언더 올인이면 이미 액션한 사람을 잠그고, 정상 레이즈면 잠금을 푼다 |
| `js/game.js` | `legalFor`가 잠긴 사람에게 레이즈를 막고, 잠긴 상태에서 들어온 레이즈 요청은 콜로 안전하게 처리 |
| `js/game.js` | `returnUncalled()` 추가 — 베팅 라운드가 끝날 때마다 최고 베팅과 2위 베팅의 차액을 되돌려 준다 |
| `js/ai.js` | 엔진이 계산한 `canRaise`를 그대로 사용하도록 변경 (기존에는 자체 추정이라 잠금 상태를 몰랐다) |

검증 결과: 족보 판정 통과, 40판 진행 통과. 실제로 초과 베팅 반환 17회 · 언더 올인 레이즈 잠금 1회가 발동했고 칩 총합은 계속 보존됐다.
